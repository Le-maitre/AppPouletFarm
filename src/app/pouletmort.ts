export interface PouletMort {
    id: number;
    causeDeces: string;
    nombre: number; // Correspond à la propriété "nombre" dans le backend
    datePerte: Date; // Correspond à la propriété "date_perte" dans le backend
}
