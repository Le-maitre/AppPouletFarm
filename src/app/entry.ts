export interface Entry {
    id: number;
    nom: string;
    nombrePoussins: number;
    dateEntree: Date; // Define dateEntree as Date type
    dateSortie: Date; // Define dateSortie as Date type
    // Other properties if present
  }
  